//let usename = 'Frank';
//let age = 30;
//age = 10;

//console.log(age)

function mostrarUsuario (userName, age){
    return `The username is: ${userName}, and age is ${age}`;
}

const mostrarUsuario2 = (userName, age) => `The username is: ${userName}, and age is ${age}, whit a arrow function`;
//^-- Función flecha en JS

console.log(mostrarUsuario2('Fsjaimes', 20));